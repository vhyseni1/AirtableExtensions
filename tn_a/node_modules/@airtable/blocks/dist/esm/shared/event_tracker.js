import getAirtableInterface from '../injected/airtable_interface';
/** @hidden */
export function trackEvent(eventSchemaName) {
  let eventData = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  getAirtableInterface().trackEvent(eventSchemaName, eventData);
}

/**
 * Used for logging blockInstallation.performRecordAction event
 * Mirrored from hyperbase block_installation_record_action_status.tsx
 *
 * @hidden
 * */
export let RecordActionStatus = /*#__PURE__*/function (RecordActionStatus) {
  RecordActionStatus["SUCCESS"] = "SUCCESS";
  RecordActionStatus["IGNORED"] = "IGNORED";
  RecordActionStatus["SETTINGS_INVALID"] = "SETTINGS_INVALID";
  RecordActionStatus["TABLE_MISMATCH"] = "TABLE_MISMATCH";
  return RecordActionStatus;
}({});